// This service worker is managed by next-pwa
// You shouldn't need to modify it
// It will be overwritten by the plugin

self.addEventListener('install', (event) => {
  // Skip over the "waiting" lifecycle state
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  // Claim any clients immediately
  event.waitUntil(clients.claim());
});

// Placeholder for next-pwa to generate the service worker code
// The actual code will be generated by next-pwa during build time
