import { KeyPair } from '../types';
import { Pupil } from '../types';
import { Attendance } from '.';
declare const _default: (keyPair: KeyPair, restUrl: string, pupil: Pupil, dateFrom: Date, dateTo: Date) => Promise<Attendance>;
export default _default;
