import { KeyPair } from '../types';
import { Pupil } from '../types';
import { Exam } from '.';
declare const _default: (keyPair: KeyPair, restUrl: string, pupil: Pupil, dateFrom: Date, dateTo: Date) => Promise<Exam>;
export default _default;
