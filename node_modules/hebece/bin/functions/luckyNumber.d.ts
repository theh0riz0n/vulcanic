import { LuckyNumber } from '.';
import { KeyPair } from '../types';
import { Pupil } from '../types';
declare const _default: (keyPair: KeyPair, restUrl: string, pupil: Pupil) => Promise<LuckyNumber>;
export default _default;
