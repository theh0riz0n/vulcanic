import { KeyPair } from '../types';
import { Pupil } from '../types';
import { AddressBook } from '.';
declare const _default: (keyPair: KeyPair, restUrl: string, pupil: Pupil) => Promise<AddressBook>;
export default _default;
