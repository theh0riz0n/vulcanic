import { KeyPair } from '../types';
import { Pupil } from '../types';
import { ChangedLesson } from '.';
declare const _default: (keyPair: KeyPair, restUrl: string, pupil: Pupil, dateFrom: Date, dateTo: Date) => Promise<ChangedLesson>;
export default _default;
