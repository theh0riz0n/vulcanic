import { KeyPair } from '../types';
import { Pupil } from '../types';
import { Message } from '.';
declare const _default: (keyPair: KeyPair, restUrl: string, pupil: Pupil, type: number, amount: number) => Promise<Message>;
export default _default;
