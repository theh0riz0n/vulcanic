import { Homework } from '.';
import { KeyPair } from '../types';
import { Pupil } from '../types';
declare const _default: (keyPair: KeyPair, restUrl: string, pupil: Pupil, dateFrom: Date, dateTo: Date) => Promise<Homework>;
export default _default;
