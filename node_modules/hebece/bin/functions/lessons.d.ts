import { KeyPair } from '../types';
import { Pupil } from '../types';
import { Lesson } from '.';
declare const _default: (keyPair: KeyPair, restUrl: string, pupil: Pupil, dateFrom: Date, dateTo: Date) => Promise<Lesson>;
export default _default;
